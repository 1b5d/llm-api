models_dir: /models
model_family: gptq_llama
setup_params:
  repo_id: repo_id
  filename: model.safetensors
model_params:
  group_size: 128
  wbits: 4
  cuda_visible_devices: "0"
  device: "cuda:0"
  st_device: 0
